[package]
name = "gomoku-rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "gomoku_rust"
crate-type = ["cdylib"]

[dependencies]
# JNI 绑定库 - 使用稳定版本
jni = "0.21"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
